{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/hooks/authorize-edit-site/context.json",
  "title": "AuthorizeEditSiteContext",
  "description": "The context of the createSite request. Either adminUser or user will be set.",
  "type": "object",
  "properties": {
    "input": {
      "type": "object",
      "properties": {
        "id": {
          "description": "The ID of the site being edited.",
          "type": "string"
        }
      },
      "required": ["id"]
    },
    "adminUser": {
      "$ref": "../../request-context/request-context-admin-user.json"
    },
    "user": {
      "$ref": "../../request-context/request-context-user.json"
    },
    "site": {
      "$ref": "../../request-context/request-context-site.json"
    }
  },
  "required": ["input", "site"]
}
